@page
@model MyEvenement.Pages.Statuts.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Statut[0].StatutName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Statut) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StatutName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.StatutID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.StatutID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.StatutID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
